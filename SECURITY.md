# Security

This project uses bring-your-own API keys. Never commit API keys, Android keystores, `local.properties`, recordings, generated notes, or build artifacts.

If you find a potential secret, do not open an issue containing its value. Report it privately to the repository owner and rotate the affected credential.
