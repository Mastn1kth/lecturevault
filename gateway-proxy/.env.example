# Public DNS name pointing at this server. Caddy obtains HTTPS automatically.
PUBLIC_HOST=api.example.ru

# The current secure AI gateway. This is not an AI key.
UPSTREAM_URL=https://lecturevault-ai-gateway.aleksandrsimunin828.workers.dev

# Generate with: openssl rand -hex 32
# Put exactly the same value into Cloudflare as TRUSTED_PROXY_TOKEN.
PROXY_TOKEN=replace-with-a-long-random-secret
